// Temporary development Firestore rules
// Allow read/write for authenticated users. Do NOT use in production.

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow authenticated reads/writes to all documents (dev only)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
